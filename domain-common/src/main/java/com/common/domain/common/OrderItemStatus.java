package com.common.domain.common;

public enum OrderItemStatus {
    ORDERED,
    PAID,
    READY,
    SHIPPING,
    SHIPPED,
    CANCEL_REQUESTED,
    CANCELED,
    RETURN_REQUESTED,
    RETURNED,
    PAYMENT_FAILED,
    FAILED,
}